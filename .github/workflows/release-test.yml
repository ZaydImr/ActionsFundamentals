name: Testing the Release from hello-world-docker-action

on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: Zaydimr/hello-world-docker-action@v1
        with:
          who-to-greet: '@Zaydimr'
